json{
  "name": "nohallu-vercel",
  "version": "1.0.0",
  "description": "AI hallucination detection for academic citations",
  "main": "api/verify-citations.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static build'",
    "start": "echo 'Serverless functions'"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": "18.x"
  }
}
